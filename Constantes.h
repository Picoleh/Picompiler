#pragma once

#include <vector>
#include <string>
#include <map>

using namespace std;

namespace Constantes {
    extern const vector<char> LETRAS;
    extern const vector<char> DIGITOS;
    extern const vector<string> SIMBOLOS_ESPECIAIS;
    extern const vector<string> PALAVRAS_RESERVADAS;
    extern const vector<string> TOKENS_RESERVADOS;
    extern const vector<string> SIMBOLOS_COMUNS;
    extern map<string, string> TOKENS;
    extern vector<string> TOKENS_ID;
    extern vector<string> IDS;
}